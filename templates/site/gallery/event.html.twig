{% extends 'site/siteBase.html.twig' %}

{% block title %}{{ eventName }} - Galerie - {{ parent() }}{% endblock %}

{% block main %}
<div class="container my-5">
    <div class="row">
        <div class="col-12">
            <nav aria-label="breadcrumb" class="mb-4">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="{{ path('gallery') }}">
                            <i class="fas fa-images me-1"></i>Galerie
                        </a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">
                        {{ eventName }}
                    </li>
                </ol>
            </nav>

            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1>
                        <i class="fas fa-calendar-alt me-3"></i>
                        {{ eventName }}
                        <small class="text-muted">({{ photos|length }} Bilder)</small>
                    </h1>
                </div>
                <a href="{{ path('gallery') }}" class="btn btn-outline-secondary">
                    <i class="fas fa-arrow-left me-2"></i>Zurück zur Übersicht
                </a>
            </div>
            
            {% if photos is empty %}
                <div class="alert alert-info">
                    <i class="fas fa-info-circle me-2"></i>
                    Keine Bilder für dieses Event gefunden.
                </div>
            {% else %}
                <div class="row g-3">
                    {% for photo in photos %}
                        <div class="col-6 col-md-4 col-lg-3">
                            <div class="card border-0 shadow-sm">
                                <a href="{{ photo.getImagePath() }}" 
                                   class="glightbox" 
                                   data-gallery="{{ eventName }}"
                                   data-title="{{ photo.title ?? 'Bild ' ~ photo.uuid }}"
                                   data-description="{{ photo.description ?? '' }}">
                                    <img src="{{ photo.getImagePath()|imagine_filter('gallery_thumb') }}"
                                         alt="{{ photo.title ?? 'Bild ' ~ photo.uuid }}"
                                         class="card-img-top"
                                         loading="lazy"
                                         style="height: 200px; object-fit: cover;">
                                </a>
                                {% if photo.title or photo.description %}
                                    <div class="card-body p-2">
                                        {% if photo.title %}
                                            <p class="card-text small text-muted mb-1">{{ photo.title }}</p>
                                        {% endif %}
                                        {% if photo.description %}
                                            <p class="card-text small text-muted mb-0">{{ photo.description|truncate(50) }}</p>
                                        {% endif %}
                                        {% if photo.galleryEvent %}
                                            <p class="card-text small text-primary mb-0">
                                                <i class="fas fa-tag me-1"></i>{{ photo.galleryEvent.name }}
                                            </p>
                                        {% endif %}
                                    </div>
                                {% endif %}
                            </div>
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {# GLightbox CSS #}
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/glightbox/dist/css/glightbox.min.css">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    
    {# GLightbox JS #}
    <script src="https://cdn.jsdelivr.net/npm/glightbox/dist/js/glightbox.min.js"></script>
    
    {# Gallery functionality #}
    {{ encore_entry_script_tags('gallery') }}
{% endblock %}