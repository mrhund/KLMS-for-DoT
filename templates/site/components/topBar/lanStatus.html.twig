{% if settings.get('lan.signup.enabled') and app.user is not empty  %}
        {% set user = app.user.user %}
        {% if user is not empty %}
            {% if user is not registered_user %}
                <a class="btn btn-link" href="{{ path('shop_checkout') }}"><i class="fa fa-ticket"></i> Ticket kaufen</a>
            {% else %}
<<<<<<< HEAD
                {% if not settings.get('lan.seatmap.enabled') %}
		    <a class="btn btn-link" href="{{ path('shop_checkout') }}"><i class="fa fa-ticket"></i> Bezahlt</a>
                {% else %}
                    {% if user is seated_user %}
                        <a class="btn btn-link" href="{{ url('seatmap') }}"><i class="fas fa-chair"></i> Sitzplatz {{ user | seat }}</a>
                    {% else %}
                        <a class="btn btn-link" href="{{ url('seatmap') }}"><i class="fas fa-chair"></i> Sitz auswählen</a>
                    {% endif %}
=======
                <span><i class="fa fa-ticket"></i> Ticket vorhanden</span>
            {% endif %}
            {% if settings.get('lan.seatmap.enabled') %}
                &nbsp;|&nbsp;
                {% if user is seated_user %}
                    <span><i class="fas fa-chair"></i> Sitzplatz <a href="{{ url('seatmap') }}">{{ user | seat }}</a></span>
                {% else %}
                    <a href="{{ url('seatmap') }}"><i class="fas fa-chair"></i> Sitzplatz auswählen</a>
>>>>>>> 58a62d9d1c82c145e3ac1087ac2b64914b20c57e
                {% endif %}
            {% endif %}
        {% endif %}
{% elseif settings.get('lan.signup.enabled')  %}
	<a class="btn btn-link" href="{{ path('shop_checkout') }}"><i class="fa fa-ticket"></i> Ticket kaufen</a>
{% endif %}
